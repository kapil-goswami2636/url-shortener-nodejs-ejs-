<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>URL Shortener</title>
    <link rel="stylesheet" href="/index.css" />
    <!-- CSS file link -->
  </head>
  <body>
    <div class="container">
      <h1>🔗 URL SHORTENER</h1>

      <!-- Form for user to enter URL -->
      <form action="/submit" method="POST">
        <input
          type="text"
          name="longurl"
          placeholder="Enter your long URL..."
          required
        />
        <button type="submit">Shorten URL</button>
      </form>

      <!-- Agar short URL generate hua hai toh yeh part dikhana -->
      <% if (shortUrL) { %>
      <div class="result">
        ✅ Your short URL:
        <a href="<%= shortUrL %>" target="_blank"><%= shortUrL %></a>
      </div>
      <% } %>
    </div>
  </body>
</html>
